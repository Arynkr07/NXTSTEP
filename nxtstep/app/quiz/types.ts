// app/quiz/types.ts // Comprehensive TypeScript types and useReducer state shape for Career Assessment Quiz export type SkillProficiency = 'beginner' | 'intermediate' | 'advanced'; export interface UserSkillItem { name: string; proficiency: SkillProficiency; } export type CurrencyType = 'INR' | 'USD'; export interface QuizState { currentStepIndex: number; // 0 to 13 (0-3: Phase 1, 4: Dynamic AI Follow-up, 5-8: Phase 2, 9-12: Phase 3, 13: Review) // Phase 1: Who You Are activities: string[]; // multi-select: activities making you lose track of time workEnvironment: { location: 'Remote' | 'Hybrid' | 'In-Office' | 'Any / Flexible'; teamSize: 'Solo / Micro (<5)' | 'Small Agile (5-20)' | 'Mid-size (20-100)' | 'Large Enterprise (100+)' | 'Any / Flexible'; pace: 'Fast-paced & High Iteration' | 'Steady & Structured' | 'Deep Research & Methodical' | 'Any / Flexible'; }; measurement: string[]; // max 2 selections frustrations: string; // single select // Dynamic AI Follow-up aiFollowUpQuestion: string | null; aiFollowUpAnswer: string; isGeneratingFollowUp: boolean; // Phase 2: What You Have skills: UserSkillItem[]; // skills with proficiency experience: 'Student / Fresher' | '0–1 years' | '1–3 years' | '3–5 years' | '5–10 years' | '10+ years'; projects: string; // max 200 chars: what have you built or shipped education: 'High School / Secondary' | 'Diploma / Associate' | "Bachelor's Degree" | "Master's Degree" | 'PhD / Doctorate' | 'Self-Taught / Bootcamp'; fieldOfStudy: string; // Phase 3: Where You're Going currency: CurrencyType; salaryRange: string; timeline: '3 months' | '6 months' | '1 year' | '2+ years'; openToEducation: 'Yes, full-time' | 'Yes, part-time / online' | 'No, direct job transition' | 'Maybe, depends on ROI'; openToEducationReason: string; concern: 'Job Security & Longevity' | 'Accelerated Career Growth' | 'Work-Life Balance' | 'Tangible Impact & Mission' | 'Maximizing Compensation'; // Submission & Status isSubmitting: boolean; error: string | null; } export type QuizAction = | { type: 'SET_STEP'; payload: number } | { type: 'NEXT_STEP' } | { type: 'PREV_STEP' } | { type: 'SET_ACTIVITIES'; payload: string[] } | { type: 'SET_WORK_ENVIRONMENT'; payload: Partial<QuizState['workEnvironment']> } | { type: 'SET_MEASUREMENT'; payload: string[] } | { type: 'SET_FRUSTRATIONS'; payload: string } | { type: 'SET_AI_FOLLOW_UP_QUESTION'; payload: string } | { type: 'SET_AI_FOLLOW_UP_ANSWER'; payload: string } | { type: 'SET_GENERATING_FOLLOW_UP'; payload: boolean } | { type: 'ADD_SKILL'; payload: UserSkillItem } | { type: 'REMOVE_SKILL'; payload: string } | { type: 'SET_SKILL_PROFICIENCY'; payload: { name: string; proficiency: SkillProficiency } } | { type: 'SET_EXPERIENCE'; payload: QuizState['experience'] } | { type: 'SET_PROJECTS'; payload: string } | { type: 'SET_EDUCATION'; payload: QuizState['education'] } | { type: 'SET_FIELD_OF_STUDY'; payload: string } | { type: 'SET_CURRENCY'; payload: CurrencyType } | { type: 'SET_SALARY_RANGE'; payload: string } | { type: 'SET_TIMELINE'; payload: QuizState['timeline'] } | { type: 'SET_OPEN_TO_EDUCATION'; payload: QuizState['openToEducation'] } | { type: 'SET_OPEN_TO_EDUCATION_REASON'; payload: string } | { type: 'SET_CONCERN'; payload: QuizState['concern'] } | { type: 'SET_SUBMITTING'; payload: boolean } | { type: 'SET_ERROR'; payload: string | null } | { type: 'RESET_QUIZ' }; export interface CareerRecommendationResult { title: string; fitScore: number; whyItFits: string; salaryMatch: boolean; timelineRealistic: boolean; skillGaps: string[]; firstSteps: string[]; topCompaniesHiring: string[]; } export interface AssessmentResponse { careers: CareerRecommendationResult[]; overallInsight: string; biggestStrength: string; watchOut: string; needsRefinement?: boolean; refinementMessage?: string; suggestedOptions?: string[]; } 